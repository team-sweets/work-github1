<div class="row">
 <div class="col-lg-12" >

  <h5 class="head-line title_h2 text-center">注文情報入力</h5>
    <% if current_customer.cart_items.present? %>
    <%#= render "layouts/error_messages", resource: @order%>
    <div class="col-xs-offset-1">
      <%= form_with model: Order.new, url: orders_log_path, local: true, method: :post do |f| %>

      <div class="form-group">
      <%= f.radio_button :payment_method, 0, checked: true %>
      <span>&nbsp;クレジットカード</span>


      <%= f.radio_button :payment_method, 1 %>
      <span>&nbsp;銀行振込</span>
      </div>

        <div class="form-group">
            <%= f.radio_button :addresses, "residence", checked: true  %>
            <span>&nbsp;ご自身の住所</span><br>
            &emsp;&emsp;<%= "#{current_customer.postal_code} #{current_customer.address}" %>
            <br>
            &emsp;&emsp;<%= "#{current_customer.first_name} #{current_customer.last_name}" %>
          </div>

          <div class="new-address col-xs-10 radio">
            <%= f.radio_button :addresses, "new_address" %>
            <span>&nbsp;新しいお届け先</span><br>
            <div class="field form-group col-xs-11">
              <%= f.label :"郵便番号(ハイフンなし)", class: "col-sm-4 control-label" %>
              <%= f.text_field :postal_code, class: "col-sm-3 form-control-static postal_code", placeholder: "0000000" %>
            </div>
            <div class="field form-group col-xs-11">
              <%= f.label :"住所", class: "col-sm-4 control-label" %>
              <%= f.text_field :address, class: "col-sm-8 form-control-static address", placeholder: "東京都渋谷区代々木神園町0-0" %>
            </div>
            <div class="field form-group col-xs-11">
              <%= f.label :"宛名", class: "col-sm-4 control-label" %>
              <%= f.text_field :name, class: "col-sm-4 form-control-static", placeholder: "令和道子" %>
            </div>
          </div>
         </div>
        <%= f.submit "確認画面へ進む", class: 'btn btn-primary' %>

      <% end %>
    </div>
    <% else %>
      <!-- if カートアイテムがない時 -->
      <p>お客様のショッピングカートに商品はありませんので注文情報の入力ができません。</p>
      <p><%= link_to "商品一覧", products_path, class: "btn btn-warning" %>から商品をお探し下さい。</p>
    <% end %>
  </div>
</div>



 </div>
</div>